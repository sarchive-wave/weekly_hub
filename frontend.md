# frontend.md
# AI Weekly Hub - 프론트엔드 명세
# 화면 레이아웃, 컴포넌트 구조, UI/UX 정의

---

## 페이지 목록

| 경로 | 페이지 | 접근 |
|------|--------|------|
| `/login` | 로그인 | 비로그인 |
| `/` | 메인 (주차 카드 목록) | 로그인 |
| `/weeks/:weekId` | 주차 상세 | 로그인 |
| `/settings` | 설정 | 관리자 |

---

## 레이아웃 구조

### 공통 레이아웃 (로그인 후)
```
┌─────────────────────────────────────────┐
│ AppBar: AI Weekly Hub    [설정] [로그아웃] │
├─────────────────────────────────────────┤
│                                         │
│              페이지 콘텐츠               │
│                                         │
└─────────────────────────────────────────┘
```

### 주차 상세 레이아웃
```
┌──────────────────────────────────────────────────┐
│ AppBar: AI Weekly Hub              [설정] [로그아웃]│
├────────────┬─────────────────────────────────────┤
│ 사이드바    │                                      │
│ (240px 고정)│         메인 콘텐츠                  │
│            │                                      │
│ ← 목록     │  [기본] 전체 주간보고 (읽기전용)       │
│            │  [팀원 선택 시] 개인 작성 화면         │
│ 홍길동 ●   │                                      │
│ 김철수 ●   │                                      │
│ 박영희 ●   │                                      │
│            │                                      │
└────────────┴─────────────────────────────────────┘

● 색상: 회색(미작성) / 노란색(작성중) / 파란색(완료)
```

---

## 페이지별 명세

### 1. 로그인 페이지 (`/login`)
- 중앙 정렬 카드
- 필드: 아이디, 비밀번호
- 로그인 버튼, Enter 키 제출
- 에러: "아이디 또는 비밀번호가 올바르지 않습니다"

### 2. 메인 페이지 (`/`)
- 제목: "AI Weekly Hub"
- 관리자: [+ 주차 추가] 버튼 (오른쪽 상단)
- 주차 카드 그리드 (3열, 반응형)
  ```
  ┌─────────────────┐
  │ 7월 1주차       │
  │ 2026년          │
  │ 3/5명 완료      │  ← 완료/전체 인원 수
  └─────────────────┘
  ```
- 카드 클릭 → `/weeks/:weekId`
- 관리자: 카드에 삭제 아이콘 (hover 시 표시)

### 3. 주차 상세 페이지 (`/weeks/:weekId`)

#### 사이드바
- 너비: 240px 고정
- [← 목록으로] 버튼 (상단)
- 구분선
- 팀원 이름 목록 (sort_order 순)
  - 각 항목: 이름 + 왼쪽 색상 도트 (상태 표시)
  - 선택된 항목 강조
  - 클릭 → 해당 팀원 작성 화면

#### 메인 콘텐츠 - 전체 주간보고 (기본, 팀원 미선택)
- 제목: "{title} 전체 주간보고"
- 프로젝트별 섹션 (sort_order 순)
  ```
  ┌──────────────────────────────────────┐
  │ 프로젝트명                            │
  ├──────────────┬───────────────────────┤
  │ 금주 업무    │ 차주 업무              │
  │ · 내용...   │ · 내용...              │
  │ · 내용...   │                        │
  └──────────────┴───────────────────────┘
  ```
- 내용 없는 프로젝트는 숨김
- 읽기 전용, 작성자 이름 표시 안 함

#### 메인 콘텐츠 - 개인 작성 화면 (팀원 선택 시)
- 제목: "{팀원 이름}의 주간보고"
- 상단: 상태 토글
  ```
  [작성중]  [작성완료]  ← 현재 상태가 강조
  ```
- 본인/관리자만 상태 변경 가능
- 프로젝트 선택 섹션
  - 등록된 프로젝트 목록 체크박스로 선택
  - 선택된 프로젝트별 입력 카드
    ```
    ┌─────────────────────────────────────┐
    │ 프로젝트명                          │
    ├──────────────────┬──────────────────┤
    │ 금주 업무        │ 차주 업무        │
    │ [Textarea]       │ [Textarea]       │
    └──────────────────┴──────────────────┘
    ```
- [저장] 버튼 (하단 고정)
- 본인/관리자만 수정 가능, 타인 열람 시 읽기 전용

### 4. 설정 페이지 (`/settings`) - 관리자 전용

탭 구조:
- **계정 관리**: 테이블 (아이디/이름/역할/상태) + 추가/수정/삭제/비밀번호초기화
- **팀원 관리**: 드래그 순서 변경 가능한 목록 + 추가/삭제
- **프로젝트 관리**: 드래그 순서 변경 가능한 목록 + 추가/삭제

---

## 상태 색상 정의

| 상태 | 값 | 색상 | MUI 색상 |
|------|-----|------|----------|
| 미작성 | `none` | 회색 | `grey.400` |
| 작성중 | `draft` | 노란색 | `warning.main` |
| 완료 | `done` | 파란색 | `primary.main` |

---

## 컴포넌트 구조

```
src/
├── api/
│   ├── authApi.ts
│   ├── weekApi.ts
│   ├── reportApi.ts
│   ├── projectApi.ts
│   └── userApi.ts
├── components/
│   ├── layout/
│   │   ├── AppLayout.tsx       # 공통 AppBar + 페이지 래퍼
│   │   └── WeekLayout.tsx      # 사이드바 + 메인 2단 레이아웃
│   ├── week/
│   │   ├── WeekCard.tsx        # 메인 페이지 카드
│   │   └── WeekCreateDialog.tsx
│   ├── report/
│   │   ├── OverallReport.tsx   # 전체 주간보고 (읽기전용)
│   │   ├── PersonalReport.tsx  # 개인 작성 화면
│   │   └── ProjectEntry.tsx    # 프로젝트별 금주/차주 입력 카드
│   └── settings/
│       ├── AccountTab.tsx
│       ├── MemberTab.tsx
│       └── ProjectTab.tsx
├── contexts/
│   └── AuthContext.tsx         # JWT 토큰, 유저 정보, 로그인/아웃
├── hooks/
│   ├── useAuth.ts
│   ├── useWeeks.ts
│   └── useReport.ts
├── pages/
│   ├── LoginPage.tsx
│   ├── MainPage.tsx
│   ├── WeekDetailPage.tsx
│   └── SettingsPage.tsx
└── types/
    ├── auth.ts
    ├── week.ts
    ├── report.ts
    ├── project.ts
    └── user.ts
```

---

## axios 설정

- baseURL: `http://localhost:8081`
- 인터셉터: 요청 시 Authorization 헤더 자동 추가
- 응답 401: localStorage 토큰 삭제 → `/login` 리다이렉트
