# 프론트엔드 환경설정 예시 — 복사하여 .env 로 사용
# 개발 서버 포트
VITE_DEV_PORT=5174
# 개발 시 /api 프록시가 전달할 백엔드 주소
VITE_DEV_API_TARGET=http://localhost:8081
# 배포 시 백엔드 절대주소(프론트와 다를 때만). 비우면 same-origin(/api) 사용
# VITE_API_BASE=https://api.example.com
